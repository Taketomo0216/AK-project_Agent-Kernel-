{
  "name": "ak-project-agent-kernel",
  "version": "0.2.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test dist/tests/*.test.js",
    "test:unit": "npm run build && npm run test",
    "eval:consistency": "npm run build && node dist/eval/consistency/runConsistencyEval.js"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "typescript": "^5.8.2"
  }
}
